# 本地/通用运行期配置模板。预发与生产的「连库、连 Redis」不在仓库里各建一份：
# 在预发 Deployment 与 生产 Deployment（或 SRE 上两个应用）分别注入同名变量、不同连接串。
# 构建期仅 NEXT_PUBLIC_* 见 .env.pre.example / .env.prod.example。

# 数据库（MySQL utf8mb3）
DATABASE_URL="mysql://user:pass@127.0.0.1:3306/xiaqiu?charset=utf8mb3"

NEXT_PUBLIC_SITE_URL="http://localhost:3000"

